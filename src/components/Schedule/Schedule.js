import React from 'react';
import PropTypes from 'prop-types';


const Schedule = () => (
  <div>
    Schedule Component
  </div>
);

Schedule.propTypes = {};

Schedule.defaultProps = {};

export default Schedule;
